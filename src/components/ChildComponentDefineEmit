<script setup>
const emit = defineEmits(['updateMessage']);

const emitEvent = () => {
  emit('updateMessage', 'doei');
};
</script>

<template>
  <div>
    <button @click="emitEvent">Verzend Bericht</button>
  </div>
</template>